displayName: error401
responses:
  401:
    description: Unauthorized - De gebruiker is niet ingelogd of heeft onvoldoende machtigingen om deze resource te gebruiken.
    body:
      application/json:
        schema: !include ../model/error.json
