displayName: error412
responses:
  412:
    description: PreCondition Failed - Er zijn parameters die niet voldoen aan de vormvereisten.
    body:
      application/json:
        schema: !include ../model/error.json
